@*@model IEnumerable<WebAppPersonelGorevDagitim.Models.ProsesDagitimView>*@
@model TarihPrm

@{
    ViewData["Title"] = "Index";
}


<head>
    <!--

    <script type="text/javascript" src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.8.3.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.0.3/js/bootstrap.min.js"></script>
    <link rel="stylesheet" media="screen" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.0.3/css/bootstrap.min.css" />
    <link type="text/css" rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.4/css/bootstrap-datepicker.css" />
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.6.4/js/bootstrap-datepicker.js"></script>

    -->
</head>


<h1>Index</h1>

<p>
    <a asp-action="Create">Liste Oluştur</a>
    <br />

    @using (Html.BeginForm("Index", "ProsesDagitim", FormMethod.Post))
    {
<div>
    Tarih:
    @*@Html.TextBox("Tarih", ((DateTime)ViewBag.fromDate).ToString("dd/MM/yyyy"), new { @class = "DatePicker", @readonly = "readonly" })*@
    @*<input value="@(((DateTime)ViewBag.fromDate).ToString("dd/MM/yyyy"))"  class = "DatePicker" name="tarihPrm" readonly  />*@

    <!-- 
    <input asp-for="@Model.Tarih" asp-format="{0:dd.MM.yyyy}" class="DatePicker" readonly />
    -->
    
    <!-- tarihteki display sorunundan dolayi Tarih_ToString kullanildi, yoksa sadece Tarih yeterliydi   -->
    <input asp-for="@Model.Tarih_ToString" asp-format="{0:dd.MM.yyyy}" class="DatePicker" readonly />
    <input asp-for="@Model.Tarih" type="hidden" />
</div>
        <br />

        <input type="submit" value="Listele" class="btn btn-primary" />
        <span> </span>
    }
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().PDId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().Tarih)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().PersonelAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().PersonelSoyadi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().ProsesAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.prosesDagitim.First().Zorluk)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.prosesDagitim)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PDId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Tarih)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PersonelAdi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PersonelSoyadi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProsesAdi)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Zorluk)
                </td>

                <!--
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>

                -->
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}


<script type="text/javascript">
        $(function () {

        });

        $(".DatePicker").datepicker({
            autoclose: true,
            format: 'dd.mm.yyyy',
            language: 'tr'

        }).on('changeDate', function (selected) {          
            $("#Tarih").val($(this).val());
         });

        
</script>
}